server:
  port: 8083

spring:
  application:
    name: isa-order
  data:
    mongodb:
      host: localhost
      port: 27017
      database: isa1
      auto-index-creation: true

logging:
  level:
    org.springframework: DEBUG
  file:
    name: /tmp/logs/${spring.application.name}.log